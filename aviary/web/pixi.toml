[workspace]
name = "aviary-web"
channels = ["conda-forge"]
platforms = ["linux-64"]

[dependencies]
python = ">=3.8"
flask = ">=2.3"

[activation]
env = { PYTHONNOUSERSITE = "1" }

[tasks]
server = { cmd = "python server.py", description = "Start with defaults (port 8090, cwd as root)" }
